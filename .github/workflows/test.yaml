name: 测试例程

on: push

jobs:
  Spear-build:
    name: 编译Spear相关文件
    runs-on: ubuntu-latest
    steps:
      - name: 读取仓库内容
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23' # 请根据需要选择合适的 Go 版本

      - name: Verify Go version
        run: go version

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'
      
      - name: Verify Python version
        run: python --version

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: 安装依赖并且编译文件
        run: |
          sudo apt install portaudio19-dev libx11-dev libxtst-dev
          curl -fsSL https://get.docker.com -o get-docker.sh
          sudo sh get-docker.sh
          docker version
          make

      - name: Get Ubuntu IP Address
        run: |
          ip_address=$(ip addr show eth0 | grep -oP 'inet \K[\d.]+')
          echo "IP Address: $ip_address"
          export SERVICE_IP=$ip_address
          echo "SERVICE_IP=$ip_address" >> $GITHUB_ENV
          OPENAI_API_KEY='sk-tgSX19ayjMdHQqBxLkJrsVbgoHzJUteUlOYzxCnrkYvmgvjk'
          HUGGINGFACEHUB_API_TOKEN='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'
          export OPENAI_API_KEY='sk-tgSX19ayjMdHQqBxLkJrsVbgoHzJUteUlOYzxCnrkYvmgvjk'
          export HUGGINGFACEHUB_API_TOKEN='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'
          echo "SERVICE_IP=$ip_address" >> $GITHUB_ENV
          echo "OPENAI_API_KEY='sk-tgSX19ayjMdHQqBxLkJrsVbgoHzJUteUlOYzxCnrkYvmgvjk'" >> $GITHUB_ENV
          echo "HUGGINGFACEHUB_API_TOKEN='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'" >> $GITHUB_ENV
          ls -l
          go test -v ./test/simple_req_test.go